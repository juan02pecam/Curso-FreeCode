let count = 0 ;
function cardCounter (card){
    if (card === Number) {
        if(card >= 2 && card <= 10){
            card = (2, 3, 4, 5, 6) + 1;
        }
        
    } else if (typeof card === "string"){
        
    }
}